# Changer Music Tag

**Changer Music Tag** — это легковесный и мощный инструмент на Python для глубокого парсинга и визуализации метаданных MP3-файлов формата ID3v2.

Приложение позволяет не только просматривать стандартные текстовые теги (артист, альбом, год), но и извлекать встроенные изображения, обрабатывать сложные структуры комментариев и корректно работать с различными кодировками (UTF-8, UTF-16, Latin-1).

## ✨ Основные возможности

* **Глубокий парсинг**: Чтение заголовков ID3v2.3 и ID3v2.4.
* **Поддержка различных типов данных**:
* Текстовые фреймы (Названия, Артисты, Жанры).
* Комментарии (`COMM`) с автоматическим определением языка и кодировки.
* Изображения (`APIC`) с возможностью автоматического сохранения на диск.


* **Гибкое форматирование**: Два режима вывода данных в консоль:
* `Table`: Строгая структура с разделителями для детального анализа.
* `KeyValue`: Минималистичный список для быстрой проверки.


* **Умная работа с кодировками**: Исправляет проблему «кракозябр» и иероглифов при чтении UTF-16 с BOM.

## 🚀 Быстрый старт

### Требования

* Python 3.10 или выше

### Установка

1. Клонируйте репозиторий:
```bash

git clone https://github.com/ncesam/changer-music-tag.git
cd changer-music-tag

```



### Использование

Простой пример запуска парсера:

```python
from ID3Parser import ID3Parser
from Formatter import Formatter

# 1. Открываем файл
with open("track.mp3", "rb") as f:
    # 2. Парсим данные
    info = ID3Parser.parse(f)
    
    # 3. Выводим красиво в консоль (режим Table)
    formatter = Formatter(Formatter.Mode.Table)
    formatter.pretty_print(info, padding=1, filename=f.name)

```
---

## 🛠 Архитектура проекта

* `ID3Parser.py`: Ядро системы. Содержит логику чтения байтов, перемещения курсора (`seek/tell`) и декодирования фреймов.
* `Formatter.py`: Модуль визуализации. Отвечает за выравнивание текста и интерфейс пользователя.
* `Frame.py`: Содержит Enum-классы и структуры данных (`dataclasses`) для идентификаторов фреймов и категорий.

## 📸 Работа с изображениями

При обнаружении фрейма `APIC` (Attached Picture), приложение:

1. Извлекает бинарные данные изображения.
2. Определяет MIME-тип (JPEG, PNG).
3. Присваивает уникальное имя файла на основе типа обложки и название файла.
4. Сохраняет файл в корневую папку проекта.

---

## RoadMap

* [ ] Редактирование тегов и сохранение изменений обратно в файл.
* [ ] Поддержка пакетной обработки (сканирование всей папки с музыкой).
* [ ] Графический интерфейс на основе Tkinter или PySide.

## Contributing:
Приветствуются Pull Requests! Если вы нашли баг или добавить новые функционал, создавайте Issue.

**Разработал:** Ncesam

**Лицензия:** MIT
